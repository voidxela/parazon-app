#!/command/execlineb -P
# Called when the api process exits. Log the exit code for diagnostics.
# $1 = exit code, $2 = signal number (or 256 if not killed by signal)
foreground { s6-echo "api exited with code ${1}" }
