#!/command/execlineb -P
# Called when the vector-indexer process exits. Log the exit code for diagnostics.
# $1 = exit code, $2 = signal number (or 256 if not killed by signal)
foreground { s6-echo "vector-indexer exited with code ${1}" }
